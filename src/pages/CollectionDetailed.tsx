import React from "react";
import CollectionViewerContainer from "../containers/CollectionViewerContainer";

const CollectionDetailed: React.FC = () => <CollectionViewerContainer />;

export default CollectionDetailed;
